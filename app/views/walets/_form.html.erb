<%= form_for(@walet) do |f| %>
  <% if @walet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@walet.errors.count, "error") %> prohibited this walet from being saved:</h2>

      <ul>
      <% @walet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
