<p #{'Bits show'}
%h1
  = @event.title
  %h3
    %td= @event.status
    %td= @event.minbits
    - if can? :manage, Bit
      = button_to 'Edit', edit_event_path(@event)
      
    = link_to 'Back', events_path

  