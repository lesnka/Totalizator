
 <p <%='Bits show'%></p>
  

            <h2><%=@event.title %><h2>
            <dt><%=@event.status %></dt>
            <dt><%=@event.minbits %></dt>
<% if can? :manage, Bit %>
<%= link_to 'Edit', edit_event_path(@event) %> |
<%end%>
<%= link_to 'Back', events_path %>
