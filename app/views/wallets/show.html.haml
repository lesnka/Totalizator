- if notice
  %p#notice= notice
%tr
  %h1
    Your balance
    %h1
      - if @wallet.user_id == current_user.id
        %h2= @wallet.balance
        %h2
          = button_to 'Edit', edit_wallet_path(@wallet), :method => :get, :class => "btn btn-danger", id: "edit_balance"
        %h2
          = link_to 'Back', events_url, :class =>"btn btn-primary "
          
